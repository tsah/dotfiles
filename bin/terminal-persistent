#!/bin/bash
# Launch or focus persistent terminal

# Check if persistent terminal window exists
if hyprctl clients | grep -q "title: Persistent Terminal"; then
    # Focus existing persistent terminal window
    hyprctl dispatch focuswindow "title:Persistent Terminal"
else
    # Launch new terminal instance with persistent title
    exec uwsm app -- ghostty --title="Persistent Terminal" "$@"
fi