#!/bin/bash
# Launch Chromium with the Work profile

exec chromium \
    --ozone-platform=wayland \
    --ozone-platform-hint=wayland \
    --enable-features=UseOzonePlatform,TouchpadOverscrollHistoryNavigation \
    "$@"
