#!/bin/sh
# Waystation workspace navigator and headless agent API.
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ "${1:-}" = "agent" ]; then
    shift
    exec "$script_dir/dotfiles-workflow" waystation-agent "$@"
fi
exec "$script_dir/workspace-picker" "$@"
